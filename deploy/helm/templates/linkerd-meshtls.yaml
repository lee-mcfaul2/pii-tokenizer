apiVersion: policy.linkerd.io/v1alpha1
kind: MeshTLSAuthentication
metadata:
  name: agent-gateway-only
  namespace: {{ .Values.namespace }}
spec:
  identities:
    - "agent-gateway-sa.gateway.serviceaccount.identity.linkerd.cluster.local"
